---
import Header from "../components/Header.astro";
import Layout from "../components/Layout.astro";
import { SEOs as seo } from "../assets";
import Info from "../components/Info.astro";
const { title, desc, kw, icon } = seo.infos;
---

<Layout title={title} desc={desc} kw={kw} icon={icon}>
  <Header />
  <Info />
</Layout>