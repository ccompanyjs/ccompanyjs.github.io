---
import Header from "../components/Header.astro";
import Layout from "../components/Layout.astro";
import { seoHome } from "../components/seo";
const { title, desc, kw } = seoHome;
---

<Layout title={title} desc={desc} kw={kw}>
  <Header />
</Layout>